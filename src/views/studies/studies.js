import "./studies.css";

const renderStudies = () => {
  const root = document.querySelector(".root");
  root.innerHTML += `  <div class="studies">
      <h2>Studies</h2>
    </div>`;
};
export default renderStudies;
