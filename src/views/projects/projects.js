import "./projects.css";

const renderProjects = () => {
  const root = document.querySelector(".root");
  root.innerHTML += `  <div class="projects">
        <h2>Projects</h2>
        </div>`;
};

export default renderProjects;
